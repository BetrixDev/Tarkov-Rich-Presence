export const ROOT_PATH = `${process.env.APPDATA as string}\\tarkov-rich-presence`;
export const CONFIG_PATH = `${ROOT_PATH}\\config.json`;

export const TEMP_PATH = `${ROOT_PATH}\\temp`;
export const DATA_PATH = `${TEMP_PATH}\\data.json`;
